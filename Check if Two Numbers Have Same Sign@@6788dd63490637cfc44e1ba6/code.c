#include <stdio.h>

int main() {
    int a;
    int b;
    scanf("%d %d",&a,&b);
    if(a==+ || b==+) {
        printf("Same sign");
    }else {
        printf("Different Sign");
    }

    return 0;
}