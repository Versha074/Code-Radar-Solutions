#include <stdio.h>

int main() {
    int a,b;
    scanf("%d",&a);
    scanf("%d",&b);
    if(number &(1<<n)) {
        printf("1",a);
    } else {
        printf("0",b);
    }

    return 0;
}