#include <stdio.h>

int main() {
    int number;
    scanf("%d",&number);
    if(a>0) {
        printf("Positive");
    } else if(a<0) {
        printf("Negative");
    } else {
        printf("Zero");
    }
    }
}